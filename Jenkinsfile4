pipeline {
    agent any

    tools {
        maven 'maven-3.9.12'
    }

    stages {
        stage('Build Application') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/architectruslann-cpu/java-tomcat-sample.git'

                sh 'mvn clean package'

                archiveArtifacts artifacts: '**/*.war'
            }
        }

        stage('Deploy to Staging') {
            steps {
                deploy adapters: [tomcat9(
                    credentialsId: 'tomcat',
                    path: '',
                    url: 'http://44.222.65.248>:8080'
                )],
                contextPath: '/',
                war: '**/*.war'
            }
        }

        stage('Deploy to Production') {
            steps {
                timeout(time: 5, unit: 'DAYS') {
                    input message: 'Approve PRODUCTION Deployment?'
                }

                deploy adapters: [tomcat9(
                    credentialsId: 'tomcat',
                    path: '',
                    url: 'http://44.200.194.22:8080'
                )],
                contextPath: '/',
                war: '**/*.war'
            }
        }
    }
}
